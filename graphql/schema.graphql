type Task {
	id: ID!
	title: String!
	category: Category
}

type Category {
	id: ID!
	name: String!
}

type Log {
	task: Task!
}

type Query {
	allTasks: [Task]
	allCategories: [Category]	
	allLogs: [Log]
}